[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    unit: Deterministic unit tests (no LLM, no external services)
    integration: Full pipeline with mocked LLM
    edge: Edge case and boundary tests
    api: FastAPI endpoint tests
